/**
 * 编辑器常量定义
 */

/** 开始节点默认 ID */
export const START_NODE_ID = 'start';

/** 循环节点 header 高度（子节点不能移入此区域） */
export const LOOP_HEADER_HEIGHT = 60;

/** 默认节点尺寸 */
export const DEFAULT_NODE_SIZE = {
	width: 240,
	height: 100,
};

/** 循环节点默认尺寸 */
export const LOOP_NODE_SIZE = {
	width: 500,
	height: 350,
};

/** 节点间距 */
export const NODE_SPACING = {
	horizontal: 280,
	vertical: 100,
};

/** 目标 handle 默认偏移量 */
export const TARGET_HANDLE_OFFSET = 28;

/** 循环节点内边距 */
export const LOOP_NODE_PADDING = 40;
