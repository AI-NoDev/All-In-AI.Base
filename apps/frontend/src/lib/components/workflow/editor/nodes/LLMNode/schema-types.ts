/**
 * Schema types for LLM Node structured output
 * Re-exports from @qiyu-allinai/json-schema-editor for convenience
 */

export {
  type SchemaType,
  type LegacyField as Field,
  type RootSchema,
  createRootSchema,
  legacyCreateField as createField,
  legacyGenerateId as generateId,
} from '@qiyu-allinai/json-schema-editor';
