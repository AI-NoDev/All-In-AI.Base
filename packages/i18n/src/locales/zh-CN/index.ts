import { common } from './common';
import { error } from './error';
import { validation } from './validation';
import { db } from './db';

export const zhCN = {
  common,
  error,
  validation,
  db,
};
